# Environment secrets — NEVER commit these
.env

# Node dependencies (server only — not needed for static deployment)
node_modules/

# Server files (Phase 5 — not part of static deployment)
server.js
package.json
package-lock.json
.env.example

# macOS
.DS_Store

# Backup / scratch files
backup_index/
content.jsbackup.text
css2
portfolio.html
Helen — Portfolio_files/

# Source PDFs (large — keep locally only)
academicpapers/

# Claude Code project files — not needed for website
CLAUDE.md
.claude/

# Unused figures — kept locally but not deployed
figures/Box2_accompanying_figure.png
figures/Fig1_the_structure_determination_workflow.png
figures/Figure3.png
figures/Polymorphs of acetaminophen.png
figures/pharm_microstructure.jpg
